# Handoff claude-master-watch — 16/09/2026, 04:22

## 1. Task and outcome

- Repo pubblico `frsorrentino/claude-master-watch` (storia riscritta, screenshot veri fuori, percorsi anonimizzati);
  il vecchio è `claude-master-watch-archivio`, privato. Secret di firma rimessi, CI verde.
- README dell'app rifatto: card d'apertura, plugin come requisito, «No Wear OS watch?» con watchOS non supportato e
  Telegram solo notifiche, 10 schermate, card del bus cifrato, setup.
- Terminale «copione» + aggiornamento dal vivo (T1-T6 confermate da Franz).
- Fase 1, blocco 1 (`e8e6393`): numeri che rotolano, archi all'ingresso nell'inquadratura, bordo HIGH che respira,
  bagliore mentre lavora, filo nel colore della sessione, icone sui tasti. G1-G8 e G10 confermate; G9 e G11 corrette
  in `9b806ad`, da riprovare.
- `bc16de8`: «segui» immediato (Repo.optimistic), corona a scatti sulla Domanda, icone nel Menu.
- `44381b5` + `451028c`: conferma nostra (`CmConfirm`) al posto dei dialoghi di sistema, riquadro quota nella Scheda,
  anello concentrico 5h/settimana, aptica TOGGLE_ON/OFF.
- Contratto 1.11 integrato (`00c16eb`) e riquadro completo con contesto, modello, effort (`596db7a`).
- Checklist nuove: H1-H6 (`8899d5e`), K1-K5 (`80fe970`). Snapshot del solo riquadro (`9ec4879`).

## 2. Decisions and why

- Bottoni azzurro pastello `#D3E3FD` su testo `#0A2050`: il corallo di Claude `#CC785C` resta ai punti di marchio
  (icona, splash, tasto Menu, anelli della tile). Motivo: i caldi in app significano «attesa» e «errore».
- Conferme: componente nostro, non `SuccessConfirmationDialog`. Frasi simmetriche «Ti avviso» / «Non ti avviso».
- Contratto 1.11 solo campi; op `model`/`effort` alla 1.12 con ripristino (b) se affidabile, altrimenti (c).
- Negli snapshot il gauge non anima (`animateOverride = false`): Paparazzi fotografa il primo fotogramma.
- B12 (overscroll) non serviva: già default in 1.6.2. `Button` non ha `animatedShapes` nella 1.6.2: per le opzioni
  serve `ButtonGroup`.

## 3. Verified facts

- Core: 360 test, 0 fallimenti (`:core:testDebugUnitTest`).
- Build wear locale: solo in primo piano, `--no-daemon --max-workers=1 -Dorg.gradle.jvmargs=-Xmx1600m
  -Pkotlin.compiler.execution.strategy=in-process :wear:assembleRelease -x lintVital*`; in background il sistema la
  ferma per memoria. 4-8 minuti.
- Sul polso c'è `09fb0d6`, installato alle 03:53:16, 0 crash. La build `596db7a` (APK delle 04:13) NON è installata:
  dalle 04:13 l'orologio non risponde né al ping né in mDNS.
- Gli snapshot della Quota mostrano i due anelli solo dopo `cc708f1`+`09fb0d6`; nella Scheda intera contesto, modello
  ed effort finiscono sotto il bordo tondo, per questo esiste `sessionContextBox`.
- claude-master: plugin `578d9e6` (contratto 1.11), bot Telegram interattivo ritirato, README con il link all'app.
  Release 0.4.10 preparata ma NON pubblicata: serve il «pubblica» diretto di Franz.

## 4. Open

- Prove al polso da fare con Franz: F1-F6, G9, G11, H1-H6, K1-K5.
- Installare `596db7a` appena l'orologio torna raggiungibile (debug wireless da riattivare).
- Fase 1 restante: 18 sì/no affiancati (`ButtonGroup`), 21 barra di lettura, 22 recap riga per riga, 23 ambient,
  24 splash animata, 25 swipe fra le Schede, 26 `SwipeToReveal`, 27 controlli Wear nelle Impostazioni, 42 moduli
  colorati, 45 forma espressiva del Menu, 47 card suggerimento, 48 giro «due secondi», 33-36 pagina Quota con
  «Ritmo della finestra», 37 banda della giornata.
- Fase 2 (tile «Domanda» e «Quota», tre complicazioni), fase 3 (1.12: modello ed effort selezionabili), fase 4.
- Da riscontri: Timeline tagliata dalla curva; pillole usate per cose non toccabili.
- Con Franz alla luce: colore dinamico dal quadrante (tocca tutta la palette).
- Icona: prompt pronti per ChatGPT e Gemini in `docs/icona/prompt-icona.md`; nessuna proposta ancora scelta.

## 5. Paths touched

- `docs/plans/2026-09-15-grafica-proposte.md` (49 proposte), `docs/plans/2026-09-16-grafica-piano.md` (fasi),
  `docs/plans/2026-09-15-terminale-copione-design.md`, `docs/verifiche/fase-5-lettura.md` (T, G, F, H, K),
  `docs/icona/prompt-icona.md`.
- core: `rules/{TerminalText,TerminalLive,QuotaHistory,DayBars,BriefCards}.kt`, `contract/Model.kt`, `data/Repo.kt`.
- wear: `ui/components/{CmConfirm,SessionQuotaCard,RollingText,FitName,CmTimeText,Gauge,BriefCard,WideButton,
  SpeakButton,SessionBadge,SessionRow,SessionHeader}.kt`, `ui/theme/{Theme,Glow}.kt`, `ui/screens/*`,
  `MainActivity.kt`, `haptics/Haptics.kt`, `tile/CmTileService.kt`, `res/values{,-en}/strings.xml`.

## 6. Invoke next

- `git -C ~/Desktop/workspaces/personali/claude-master-watch log --oneline -5` (atteso `9ec4879`).
- Leggere `docs/plans/2026-09-16-grafica-piano.md` (fasi) e `docs/verifiche/fase-5-lettura.md` (prove aperte).
- Memoria `watch-install-release-signing` prima di ogni installazione; `watch-battery-measurement` per la batteria.
- `superpowers:test-driven-development` su ogni regola del core; `superpowers:verification-before-completion`.

## 7. Date and what invalidates this handoff

16/09/2026 04:22. Scade con: l'installazione di `596db7a` sul polso, gli esiti delle prove F/G/H/K, il contratto 1.12,
la pubblicazione della release 0.4.10 del plugin, o una scelta di Franz sull'icona o sul colore dinamico.
