# Handoff 16/09/2026 (sera) — claude-master-watch

## 1. Task and outcome
- App Wear OS: giornata di rifinitura grafica e funzioni, tutto installato sul Pixel Watch 5 di Franz e committato
  (ultimo commit app `b827e41`, ultimo commit in assoluto `b51105c`). Nessun push.
- Contratto 1.11.1, 1.12 (model/effort per sessione), 1.13 (launch con `text`, `session` nel result, `projects[].last_used`)
  integrati; claude-master ha pubblicato plugin 0.4.12 (1.12) e ha pronta la 0.4.13 (1.13), pubblicazione di Franz.
- Video promozionali: due giri di prova fatti, **piano approvato come direzione ma non ancora eseguito**:
  `docs/plans/2026-09-16-video-promo-piano.md` (cosa) e `docs/plans/2026-09-16-video-promo-implementazione.md` (come, 7 task).

## 2. Decisions and why
- Menu tolto (Timeline, Recap, Notte, pagina Menu): lista sessioni con sole aperte + tasto «Chiuse di recente · N», in fondo
  Panoramica, Impostazioni, «Nuova sessione» come EdgeButton. «Quota» → «Panoramica».
- Card quota identica in Panoramica e Scheda: titolo «Quota» + segno account **dopo** il titolo (cerchio personale,
  quadrato lavoro, vuoti 10 dp); sempre due anelli; reset settimanale nella pillola «7d N % · gio 04:00».
- Notifiche senza emoji ❓/✗/✓: icone vettoriali (fumetto ambra, stop rosso spento, spunta, play) e stato a parole.
- Sigle testo per account scartate: Franz ha preferito i segni vuoti (xfamily Gemini+Codex consigliavano LAV/PER).
- Porting Apple Watch: sconsigliato ora (riscrittura quasi totale, niente Mac né dispositivo); eventuale MVP notifiche+widget.
- Video: composizione `ffmpeg` con cassa Pillow **scartata** da Franz; si torna alla cassa HTML (nera opaca, vetro).
  Scorrimento solo **guidato dall'app** (niente corona né swipe: a scatti). Montaggio in due fasi: revisione HTML, poi
  **Remotion** (già funzionante in `~/Desktop/workspaces/personali/video/storytelling-video`, da copiare, non toccare).
- Storia unica concatenata «Il rilascio 2.8.0, dal polso» (9 scene, ~75 s), non flussi isolati (Franz 18:56).
- Fixture `contract/*.json` mai modificate (R0 di claude-master): nomi/testi demo solo in `wear/.../DemoText.kt`.

## 3. Verified facts
- Core: 396 test verdi (ultimo run 16:36). Release installata alle 18:28 circa, 0 crash all'avvio.
- Orologio: `192.168.86.34`, porta attuale **33727** (cambia; `avahi-browse -rpt _adb-tls-connect._tcp`). Se TLS rifiuta
  (`SSL_peek failed`): `adb kill-server` + `qemu-x86_64-static ~/android-sdk/platform-tools/adb server nodaemon`.
- Orologio **ripristinato** (dati veri, locale di sistema, `screen_off_timeout` 600000) dopo l'ultima registrazione.
- Demo via adb (solo release installata): `--ez demo true|false`, `--ei scroll_px N --ei scroll_ms M`, locale per app
  `cmd locale set-app-locales it.pixelbox.cmwatch --locales en-US`. `uiautomator` non vede i testi Compose.
- `screenrecord` ok (480×480, VFR → ricodificare a 30 fps costanti). All'avvio a freddo compaiono quadrante personale e
  schermata precedente (una volta il debug wireless con indirizzo e porta): tagliare sempre, controllare tutta la clip.
- Build locale ~7-9 min; test core ~3-5 min; OOM possibile con due Gradle o `-Xmx1600m` (usare 1400m).
- Pagina di prova video: artifact `https://claude.ai/artifact/8v3N9exfyGDoCpYNS1HuVt` (v3, clip reali sotto cassa HTML);
  Franz ha segnalato «non accessibile» una volta, la versione autonoma inviata funziona.

## 4. Open
- **Da eseguire**: piano video Task 1-7 (prima Task 1 «Write» come ultima voce di lista — difetto reale dell'app, visibile
  nelle clip; poi Task 2 core TDD `DemoStep`).
- **Decisioni di Franz (19:14)**: dettatura simulata; nessuna voce, solo tasto ▶ acceso; fasi HTML→Remotion confermate;
  orologio da preparare **stasera** con lui (quadrante neutro, nostra tile al primo posto, debug wireless chiuso) prima
  di registrare.
- Difetti noti non ancora aperti: il numero curvo accanto all'ora sembra «7» invece di «1»; snapshot Paparazzi non
  ri-registrati da giorni; lint vital saltato in tutte le build locali; regole Timeline/Recap rimaste nel core con i test.
- Proposte non eseguite: tenere l'ultimo valore buono di contesto dopo una compattazione (lampeggio); selettore modello/
  effort nella Scheda (1.12 lato PC pronta); invio di «Nuova sessione» già con 1.13 lato app fatto.
- `docs/handoff-2026-09-14.md`, `docs/recap.md`, `wear/\001` non tracciati: non toccati, non miei.

## 5. Paths touched
- App: `wear/.../ui/screens/{Quota,Session,Sessions,Terminal,Settings,Launch}Screen.kt`, `ui/components/{WorkCards,
  BriefCard,Gauge,SessionMetersCard,SessionQuotaCard,TextAroundTrailing,CmTimeText,DemoScroll}.kt`, `push/{Glyphs,Notifier}.kt`,
  `CmApp.kt`, `MainActivity.kt`, `DemoText.kt`, `res/values*/strings.xml`, `res/drawable/ic_state_*.xml`.
- Core: `rules/{WorkPanel,BriefCards,SessionMeters,ModelText,TileTexts,NotificationPlan,ComplicationTexts,TransportChoice,
  Phase4Rules,ViewState}.kt`, `data/{Repo,Db,Store}.kt` (Room v2 + migrazione 1→2), `transport/{Rtdb,FakeTransport}.kt`,
  `contract/Model.kt`, `settings/Prefs.kt`; schema `core/schemas/.../2.json`.
- Strumenti: `tools/promo/{record.sh,compose.sh,frame.py,find.py}`. Scratchpad della sessione (può sparire): clip e pagina.

## 6. Invoke next
- Leggere `docs/plans/2026-09-16-video-promo-implementazione.md` (decisioni già prese, vedi §4), poi
  `superpowers:executing-plans` dal Task 1. I Task 1-3 e 5 si fanno senza Franz; il Task 4 (orologio) stasera con lui.
- Prima di ogni installazione: memoria `watch-install-release-signing`. Prima di registrare: Task 4 (orologio pronto).
- Per Remotion: `CLAUDE.md` e `.claude/skills/remotion-*` in `video/storytelling-video`.

## 7. Date and what invalidates this handoff
- 16/09/2026, 19:05. Superato da: un commit che esegue il Task 1 del piano video; una nuova versione del contratto (1.14);
  una decisione di Franz che cambia la storia dei video o abbandona Remotion.
