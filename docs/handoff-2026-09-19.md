# Handoff 19/09/2026 — film promozionale, lavoro a pezzi singoli

## 1. Task and outcome
- Metodo stabilito da Franz (14:10): **un pezzo per volta**, render del solo spezzone, sua approvazione, poi il successivo. Si monta tutto solo alla fine.
- **Approvate oggi**: «One glance» (`b4563b4`), «You answer» (`ede219a`), apertura scene 1-3 (`84143e5`).
- **Bocciate**: la scheda che si volta (`flip`) e la luce della notifica (`glow`) come passaggio verso «It asks» — il codice di entrambe resta, con test, ma solo `glow` è in scaletta e va tolto o sostituito.
- Film: **127,5 battiti, 69,5 s**, 67 test verdi. Traccia `music.v9.wav` (67,61 s).
- App: demo spenta, orologio riaccoppiato al relay (dati veri), tile della scena finale fotografata (`tools/promo/out/clips3/`).

## 2. Decisions and why (non deducibili dal codice)
- **La musica è la griglia, non il contrario**: lo zoom della complication dura quanto il crescendo e la ripartenza del brano cade sul cambio scena (`exitBeats`, `musicDelayBeats`). Avevo tagliato l'introduzione: era il difetto che Franz segnalava da giorni.
- **Niente esce più dal display dopo che l'orologio si è materializzato**: è la ragione per cui `flip` è stato bocciato. Vale per qualunque proposta futura su quel passaggio.
- **Un campo di colore che copre non deve mai restare vuoto**: la scena dopo nasce sotto di lui (`bgFrom` nel Backdrop), non dopo che se n'è andato.
- **Le misure si prendono sul fotogramma, non a memoria**: i tasti erano 62 px fuori posto, la parola copiata 42 px sopra l'originale. Entrambi trovati misurando, non guardando.
- Parole dei titoli a mezzo battito con la frase ferma due battiti (approvato 14:22); la regola è nel validatore.

## 3. Verified facts
- Traccia: crescendo allungato ripetendo la prima battuta, culmine a **8,71 s = battito 16** = cambio scena; quattro battute tolte in coda; musica finisce 1,9 s prima della fine del film.
- Nella traccia originale i due stacchi brevi sono a **24,04 s e 28,40 s**; il crescendo finisce a 6,53 s.
- `deliver.sh` rende in `--gl=egl`, i tratti con ThreeCanvas in `swangle` (`gl3d.py` li ricava dalla scaletta); un render per volta, `--concurrency 1`; disco 11 GB liberi su 101.
- Audit v7: 29 voci concordate con master (`docs/2026-09-19-audit-v7.md`), piano in `docs/2026-09-19-piano-v8.md`.
- Relay: il pairing falliva perché il PC cancellava la conferma un secondo dopo averla scritta; corretto da master (`8c25770`), verificato al polso dopo il riavvio del servizio.

## 4. Open
- **Franz deve decidere**: D1 la durata (69,5 s non ancora ratificati) e D2 i due account nella Panoramica (i pannelli pescano ora dall'uno ora dall'altro). Senza risposte, i gruppi 3-5 del piano v8 non si toccano.
- **Bloccanti aperti**: il muto finale è risolto dalla traccia nuova solo finché la durata non cambia; la regola unica dei pannelli contro il display è ferma su D2.
- **Passaggio verso «It asks»: servono proposte nuove** (Franz, 17:29). Vincoli: si parte da orologio centrale con la scheda appena rientrata, si arriva a «It asks.» a sinistra e orologio a destra, e nulla deve uscire dal display.
- Da fare, non iniziati: «It speaks» (domanda corta e voce nuova, servono riprese), muro degli account in 3D al posto della scena di solo testo, colore della tapparella che resta come fondo, tile nuova in «Start the next one», chiusura.
- **Musica non decisa**: «blink n miss» (messwave) richiede Artlist **Music Pro annuale** con pubblicazione mentre l'abbonamento è attivo e canale/sito nel Clearlist; Franz valuta in alternativa una traccia Lyria dal suo Google AI Pro, che durerebbe esattamente quanto il film.
- Ramo 164 commit avanti a `origin`; il push è atto di Franz.

## 5. Paths touched
`tools/promo/remotion/src/film/`: `timeline.json|ts`, `Film.tsx`, `Backdrop.tsx`, `WordMask.tsx`, `Soundtrack.tsx`, `sound.ts`, `moves.ts`, `ui/{glow,flip,blink,heroes,takeover}.ts(x)` e relativi test · `tools/promo/audio/sposta_pausa.py` · `tools/promo/sfx.py` · `tools/promo/gl3d.py` · `remotion/public/audio/music.v9.wav` · `core/.../Order.kt`, `wear/.../DemoText.kt`, `wear/src/main/res/values*/strings.xml`.

## 6. Invoke next
1. Leggere questo file e `docs/2026-09-19-piano-v8.md`; **non** rifare l'audit.
2. Proporre a Franz 2-3 passaggi nuovi verso «It asks» prima di costruire; un pezzo per volta, spezzone reso e approvazione.
3. Skill utili: `remotion-markup` (`transitions.md`, `effects.md`, `3d.md`). `@remotion/transitions`, `three`, `motion-blur`, `noise` sono installati.
4. Scrivere a master (`SendMessage`) una riga di ripartenza: il canale è vivo e ha chiesto conferma.

## 7. Date and what invalidates this
19/09/2026, 17:30. Invalidano: le risposte a D1/D2, la scelta della traccia, l'approvazione del passaggio verso «It asks», nuove riprese dal polso.
