# Handoff 18/09/2026 (00:55) — claude-master-watch

Leggere prima `docs/handoff-2026-09-17.md` (§8-13: tutta la serata) e i due piani del film. Qui solo ciò che è nuovo.

## 1. Stato
- Film: anteprima 4 consegnata (`tools/promo/remotion/out/consegna/anteprima-v4.mp4`, 67,7 s, −14,0 LUFS). Traccia «Brick By
  Brick» definitiva; voce segnaposto maschile ok; tre quarti solo in chiusura; icona **A5, simboli al 72 %, senza disco**.
  26 test TypeScript + 9 Python verdi. `master` 68+ commit avanti a `origin`, nessun push.
- **Approvato da Franz e NON ancora fatto**: (a) portare l'icona nell'app; (b) curve con atterraggio morbido nei movimenti
  finali; (c) idee dai tre video di riferimento nelle scene da rifare.

## 2. Da fare, in ordine
1. **Curve morbide** (`moves.ts`): in `closingAt` inclinazione e allontanamento usano `inOut`; Franz vuole l'atterraggio
   morbido: uscita lunga tipo `cubic-bezier(0.16, 1, 0.3, 1)` (come `WordMask`) su tilt, allontanamento e `settle`; `diveIn`
   resta in accelerazione. Aggiornare i test di `closingAt` (i valori agli estremi non cambiano).
2. **Icona nell'app**: `wear/src/main/res/drawable/ic_launcher_fg.xml` (oggi raggiera su disco corallo, sorgente
   `docs/icona/`), sfondo `@color/ic_bg` → nero, livello monocromatico, `ic_app_mono`, `ic_tile`, e il segno viola nella
   complication. Geometria: `tools/promo/remotion/src/film/LogoMark.tsx` (spazio 480: u = 300/92, segno a 0,72, arco 130°→410°
   riempito al 70 %, corallo `#D97757`, traccia `#3A404C`). Zona sicura dell'icona adattiva: 66 dp su 108. Poi build release
   (memoria `watch-install-release-signing`), installazione, prova al polso, snapshot. Aggiornare `docs/icona/`.
3. **Orologio**: accoppiamento perso. Franz: Debug wireless → «Accoppia nuovo dispositivo» → codice + porta; poi
   `qemu-x86_64-static ~/android-sdk/platform-tools/adb pair IP:PORTA CODICE`. Il server adb va avviato con l'adb dell'SDK
   sotto qemu (`nodaemon server start`), mai con `/usr/bin/adb` (29.0.6, senza TLS): dettagli in memoria. Un server qemu è
   rimasto acceso da questa sessione.
4. Piano 2 (`docs/plans/2026-09-17-film-promo-piano-2-riprese.md`): difetti A, `record.sh` sulla tabella «da → a» (c'è già
   lo scorrimento guidato `scroll_px/scroll_ms` dell'app in demo), riprese, poi suoni Material e misure finali.

## 3. I tre video di riferimento (tavole a 2 fotogrammi/s lette il 18/09; file nello scratchpad, non conservati)
«Ask Search Anything» (Google, 87 s) · «Introducing Canvas in Gemini» (Google, 60 s) · LangEase (Zelios, 33 s).
Grammatica comune, tutta fattibile in Remotion (trasformazioni 3D CSS, SVG, sfocature):
- **Un solo componente dell'interfaccia isolato e enorme** su campo pulito (il tasto «AI Mode», il pulsante «Canvas», la
  barra degli strumenti), il resto sparito o sfocato; poi ci si allontana e si scopre l'interfaccia intera.
- **Costruire e smontare**: il componente nasce come contorno e si riempie; le card entrano sfalsate, si sparpagliano ruotate
  attorno a una scritta, si riallineano; un elemento «esce» dalla schermata e diventa protagonista.
- **Piani inclinati nello spazio** con deriva lenta e profondità di campo (documento che scorre in prospettiva, muro di
  telefoni, griglia di card); bordo di luce nei colori del marchio.
- **Testo e interfaccia si scambiano il posto**: icona dentro la frase («Any language [cartella] Instantly»), parole che
  esplodono in sillabe e si ricompongono nella parola dopo, una parola che cresce fino a riempire il quadro, numeri che
  contano dentro la frase («Even 1…10 questions»), barra di avanzamento a tutto schermo con contatore, spunta e coriandoli.
- **Transizioni**: frustate sfocate, nastri di colore, una forma (la scintilla) che attraversa il quadro e diventa il punto
  della frase finale.

**Condizione**: gli elementi dell'interfaccia vanno **ricostruiti come componenti Remotion** (card di sessione, tasti della
domanda, gauge, righe del terminale, con i colori di `Theme.kt`), non presi dalle registrazioni: solo così si deformano, si
montano e si ingrandiscono senza sgranare. Le registrazioni vere restano dentro l'orologio («gira davvero qui»).

Proposte per il nostro film (da far approvare a Franz prima di costruirle):
1. La card della sessione esce dal display, cresce inclinata a tutto quadro, l'icona di stato gira ❓→▶→✓.
2. Domanda: i tasti nascono come contorno, «1 yes» si riempie; l'anello della pressione lunga corre attorno al tasto stesso.
3. Lettura: dal ▶ parte una forma d'onda; le parole dette si compongono da sillabe.
4. Terminale: le righe escono dall'orologio su un piano inclinato con profondità di campo.
5. Quota: primo piano del gauge che si disegna, con il contatore dentro la frase.
6. Frustata corallo tra gli atti; filo di luce corallo sul bordo della cassa.

## 3 bis. Aggiornamento delle 01:20
- **Fatto e verificato**: curve ad atterraggio morbido (`soft` = Bézier 0,2·0·0·1 su inclinazione e allontanamento finali, con
  test; un commit è uscito con un test rosso per una catena di comandi senza `&&`, corretto subito dopo). **Icona nell'app**
  (`4cb50a3`): `docs/icona/gen_icona.py` genera `ic_launcher_fg`, `ic_launcher_mono`, `ic_app_mono`, `ic_tile`; sfondo nero;
  percorsi verificati rasterizzandoli (`docs/icona/anteprima-A5.png`) e `:wear:mergeReleaseResources` BUILD SUCCESSFUL.
  Mancano build release, installazione e prova al polso (orologio da riaccoppiare). Nota: a 24 dp il segno al 72 % è piccolo;
  proposta non eseguita: glifo monocromo con simboli più grandi.
- **Piano 3 scritto e approvato** (`docs/plans/2026-09-18-film-promo-piano-3-interfaccia-ricostruita.md`); primo abbozzo della
  card che esce dal display in `tools/promo/remotion/out/puntoA/`, con critica al §6 del piano: va rifatto come «stesso
  oggetto» che lascia il display, con inquadratura propria, spazio credibile, senza bordi luminosi. 28 test TypeScript verdi.
  La scaletta contiene ora l'effetto `cardOut` nella scena `list` (abbozzo: toglierlo o rifarlo prima di una consegna).

## 3 ter. La notte del 18/09 (01:20-05:30), con master in assistenza
- **Orologio riaccoppiato** (codice da Franz alle 01:22): `adb pair` con l'adb dell'SDK sotto qemu, porta di connessione da mDNS
  (44387), server qemu già acceso. **Icona nuova installata sul polso** alle 01:33 (release firmata, `e64384d1…`; copia di
  ripristino dell'APK precedente nello scratchpad, ormai perso). Non provata a vista: Franz la vede al polso, lingua di
  sistema ancora it-IT e orologio al polso, quindi niente riprese di notte.
- **Cinque dei sei momenti forti costruiti** con il metodo del piano 3 (abbozzo → tavola → critica contro il fotogramma metro →
  rifacimento), ogni giro mandato a Franz con `SendUserFile` e a master con la tavola: stato, commit e dipendenze in
  **piano 3 §8**; fotogrammi metro e giri in **§7**. Mancano i tasti (answer): la clip attuale mostra la notifica di sistema.
- Strumenti nuovi: `tools/promo/fedelta.py` (differenza tra fotogramma vero e ricostruito), `riferimenti.sh` (film di
  riferimento e fotogrammi metro, cartella ignorata), `envelope.py` (inviluppo della voce). 35 test verdi, `npm run check` ok.
- **Regole di macchina da master** (sessione del sito in parallelo): ogni `remotion render` con `nice -n 15 --concurrency 2`,
  uno per volta; a 2 due render sono crollati («Page crashed»), con 1 passano; la finale intera solo con il suo via.
- **Aperto per Franz**: (a) punto di controllo A sulla card che esce, e se durante la sosta le altre card escono a ventaglio;
  (b) la clip della lista è quella vecchia allungata di 1,5 s con l'ultimo fotogramma (`s1_list.raw.mp4` è l'originale);
  (c) la scena della quota è ferma sulla card (`freeze`) finché non c'è la ripresa nuova.

## 4. Cosa invalida questo handoff
Riprese del piano 2 fatte (le dipendenze in piano 3 §8 cadono) · tasti costruiti · giro 5 del terminale · anteprima v5 vista da Franz.
