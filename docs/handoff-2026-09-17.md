# Handoff 17/09/2026 (sera) — claude-master-watch

## 1. Task and outcome
- **Film promozionale: progetto approvato e committato** `7325231` → `docs/plans/2026-09-17-film-promo-design.md`
  (copione in tre atti ~66 s, mockup fotografico, audio, sei blocchi, verifiche). Manca il piano di lavoro a passi.
- Primo montaggio muto `0e56c50`: 49,75 s, 1920×1080, sei scene, `tools/promo/remotion/out/release.mp4` (fuori da git).
- App: Task 1-3 del piano del 16/09 fatti (`8770cd7`, `189fef3`, `2f47b81`), poi `765560e` (scene demo CALM, QUESTION,
  DEPLOYED, FOLLOWUP, TICK, BLOG) e `0a546ea` (copione `record.sh`, scene 0-8). Core 405 test verdi. Build release
  installata il 17/09 07:53, 0 crash; orologio ripristinato alle 08:10 (dati veri, italiano, timeout 600000, volume 0).
- Schermata «sessione mancante» rifatta `2ea9aa3`. Contratto 1.13.1 annotato `570c30a`; plugin 0.4.13 pubblicato da
  claude-master alle 15:28 (1.13 + 1.13.1), niente da copiare.
- master è **24 commit avanti** a `origin/master`. Nessun push.

## 2. Decisions and why
- Film: destinazione principale la pagina del sito (player), README muto come derivato, social dopo. Scelta «C»: film
  60-70 s ora, taglio 35-45 s dopo dagli stessi componenti. Testi solo in inglese.
- Audio: musica Pixabay tagliata sulle battute + suoni Material (CC-BY) + **voce dell'orologio sul ▶** con Gemini TTS
  (idea di Franz, ribalta il «nessuna voce» del 16/09). Narratore: fuori, eventuale prova a fine montaggio.
- Orologio: **mockup dalle foto di Franz** (non stock, non immagini Google). Tre quarti con riflessi veri per apertura e
  chiusura; frontale con **vetro sintetico** (la cupola riflette il telefono) per le scene di lettura; display a 0,86 del
  raggio del vetro; scontorno «per costruzione» con lo smusso lucido di ~8 px; Inter + Noto Sans Mono.
- «Gemini come orecchie» **bocciato** su campioni a risposta nota: si usano misure locali e tre ascolti di Franz.
- Lingua di sistema in inglese: la cambia Franz il giorno della registrazione (sul Pixel Watch segue il telefono).
- Scena 1 con collegamento diretto alla lista (non tile né complication: in mezzo ci sono le tile di salute). Scena 7 su
  storefront (nella demo non c'è un progetto blog).
- GitHub Actions: credito al 90 %. **Push solo su `origin`**, mai sul remote `archivio` (workflow lì disattivato da
  master). Memoria `github-actions-credit.md`.

## 3. Verified facts
- Orologio `192.168.86.34`, porta che cambia (ultima 38523): `avahi-browse -rpt _adb-tls-connect._tcp`. Il debug wireless
  è caduto tre volte in una notte; `sh()` in `record.sh` si ricollega e riprova.
- In carica le notifiche NON si aprono a schermo. Una notifica vecchia della stessa sessione rende muta la nuova (stesso
  id, «avvisa una volta sola»): `fresh` chiude l'app prima. Toccare il testo della notifica non apre l'app: si scorre
  alle azioni e si tocca «Open».
- `uiautomator dump` vede testi e aree toccabili di Compose su questa build. Pressione lunga: `input motionevent DOWN/UP`.
- Dopo tre minuti senza passi lo stato demo risulta vecchio e i tasti si spengono: `demo_step tick` lo rinfresca.
- Claude Code ferma i comandi in background «per memoria»: build e render con `setsid nohup … > log`, poi attesa in primo
  piano. Build release 8-13 min (`-Xmx1400m`, `--max-workers=1`, lint vital saltato); render 1491 fotogrammi ≈ 13 min.
- Paparazzi non gira su questa VM ARM («Failed to init Bridge»): solo Actions, e solo su `origin`.
- Remotion: `tools/promo/remotion/node_modules` è un collegamento a `video/storytelling-video` (non toccarlo).
- `scipy` di sistema non va con numpy 2.4.6: misure con numpy + `wave`. Strumenti media pronti (`media-doctor.py` ok):
  ffmpeg con loudnorm/sidechain/ebur128, faster-whisper, edge-tts, tesseract.
- La domanda letta con le opzioni («…now? 1, yes. 2, no.») dura 11,4 s con una voce di prova.
- Numeri del mockup (cerchio 0,45 px, posa 20 cm / 34°, centro +32 px, convergenza 11 %): nel documento di progetto.

## 4. Open
- **Piano di lavoro del film** (forse due piani: senza orologio = Foto + Motore + Audio; con orologio = Momenti mancanti
  + Registrazione + Montaggio).
- Difetti da indagare al polso: in «New session» i tocchi sui progetti non hanno effetto (causa ignota; ipotesi: stato
  vecchio, doppia consegna dal riprova di `sh()`, altro); pressione lunga su «yes» non riesce (opzione mezza fuori dal
  bordo); Segui sulla card (y≈170 con la card grande); conferma dopo il messaggio dettato assente nella clip.
- Contorno del tre quarti da tracciare a mano; residuo minuscolo tra ansa e cinturino nel frontale.
- Foto originali: 10 `PXL_*.jpg` nella radice del repo (ignorate, 55 MB), da spostare in una cartella materiali.
- Tile: il comando di debug ha aggiunto una tile Claude Master all'indice 6: Franz controlli se è doppia.
- Prova d'ascolto da ripetere su `gemini-3.6-flash` quando non dà 503.
- Proposte non raccolte: togliere il remote `archivio`; pubblicare i 24 commit su `origin`.
- Dal 16/09, ancora aperti: numero curvo «7» accanto all'ora, snapshot Paparazzi vecchi, lint vital, regole
  Timeline/Recap nel core.
- `docs/handoff-2026-09-14.md`, `docs/recap.md`, `wear/\001`: non tracciati, non miei.

## 5. Paths touched
- Repo: `docs/plans/2026-09-17-film-promo-design.md`, `tools/promo/{record.sh,web.sh}`, `tools/promo/remotion/{src,…}`,
  `.gitignore`, `core/.../transport/FakeTransport.kt` (+ test), `wear/.../{CmApp,MainActivity,DemoText}.kt`,
  `wear/.../ui/screens/{Session,Terminal,Question}Screen.kt`, `res/values*/strings.xml`, `contract/README.md`.
- Fuori da git, locali: `tools/promo/out/prototipi-mockup/` (script e immagini di prova del mockup),
  `tools/promo/out/prova-orecchie/` (script, specifica, risposte), `tools/promo/remotion/{out,public/scenes}`.
- Fuori dal repo: `~/Desktop/workspaces/personali/kb/strumenti/gemini-come-orecchie-audio.md`,
  `~/Desktop/workspaces/personali/docs/metodologie/mockup-fotografico-dispositivo-con-schermo.md` (+ riga nel README),
  memoria `github-actions-credit.md`. Budget della prova d'ascolto: chiuso.

## 6. Invoke next
- Leggere `docs/plans/2026-09-17-film-promo-design.md`, poi `superpowers:writing-plans`. Primo punto di controllo di
  Franz: tre fotogrammi di stile + 10 s di movimento muto, con il confronto cassa disegnata / foto.
- Per i difetti dell'app: `superpowers:systematic-debugging`. Prima di ogni installazione: memoria
  `watch-install-release-signing`. Registrazioni: `tools/promo/record.sh setup|probe|record|teardown`, sempre `teardown`.
- Per Remotion: `CLAUDE.md` e `.claude/skills/remotion-*` in `video/storytelling-video`.

## 7. Date and what invalidates this handoff
- 17/09/2026, 18:55. Superato da: il piano di lavoro del film committato; un secondo giro di foto; una decisione di Franz
  che cambia copione, mockup o audio; un contratto 1.14.
